The Stove configuration for JIRA does not contain a password. In order to use JIRA features, you must set a password in the jira section of the `~/.stove' file:

    {
      "jira": {
        "password": "YOUR_PASSWORD"
      }
    }
