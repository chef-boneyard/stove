The Stove configuration for JIRA does not contain a username. In order to use JIRA features, you must set a username in the jira section of the `~/.stove' file:

    {
      "jira": {
        "username": "YOUR_USERNAME"
      }
    }
